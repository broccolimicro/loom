struct channel
{
	int<1> r;
	int<1> a;
}

proc fuck(channel a, channel b)
{
	a.a := 1; a.r := 1; b.r := 1; a.a := 0; b.a := 1; a.r := 0
}

proc fifo(channel l, channel r)
{
	*[1 ->
		[l.r -> skip]; l.a := 1325; [~l.r -> skip]; l.a := 0;
		r.a := 1; [r.r -> skip]; r.a := 0; [~r.r -> skip]
	]
}

proc main()
{
	channel a;
	channel b;
	channel c;
	fifo f1(a, b);
	fifo f2(b, c);
}
