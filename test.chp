record differentChannel
{
	int<4> a;
	int<4> r;
}

#include "test2.chp"

/* This is a comment to be ignored.
None of this will mess up CHP generation.
Isn't that great?  */

//This won't be noticed by the compiler either.


proc main()
{
	channel a;
	channel b;
	channel c;
	fifo f1(a, b);
	fifo f2(b, c);
}